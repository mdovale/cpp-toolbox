# Agent map — templates/

You are in copy-out starters.

- Never `add_subdirectory` these into the superbuild.
- User-facing templates (`app`, `header-only-lib`, …) get `kind: template` in `catalog.yaml` and a docs page that explains how to copy them.
- `templates/catalog-entry/` is the in-tree cookie for new lessons; keep it minimal and in sync with skill `add-catalog-entry`.
