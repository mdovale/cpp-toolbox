# Tests for `toolbox`

Catch2 (or doctest) tests for `toolbox/include/toolbox/` headers. No tests yet; the first header and its test should land together.

Until Catch2 is wired, a new header still belongs with a `*_test.cpp` here so the spine can pick it up.
