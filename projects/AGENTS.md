# Agent map — projects/

You are in mini-projects.

- Path: `projects/<slug>/`, `kind: project`. Several translation units are fine; keep the reader’s first hour small.
- Still a vertical slice (README, catalog, docs `literalinclude`).
- Link learning-path tutorials; do not duplicate them.
- Follow skill `add-catalog-entry` with `kind: project`.
