# Mini-projects

Small end-to-end programs a working engineer could extend (a short simulator, a least-squares fit, a data pipeline). Folder: `projects/<slug>/`.

Projects may have several source files. They still get one catalog row, one leaf README, and a docs page. Link the tutorials the reader should know first.
