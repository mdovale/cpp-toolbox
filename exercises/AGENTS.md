# Agent map — exercises/

You are in problem / starter / solution trees.

- Path: `exercises/<topic>/<slug>/` with `starter/` and `solution/` both compiling.
- `prompt.md` has no spoilers. The leaf `README.md` may link to `solution/` but must not paste it.
- Docs: prompt in the book; solution via Sphinx dropdown (HTML) and a Solutions appendix (PDF).
- Follow skill `add-catalog-entry` with `kind: exercise`.
