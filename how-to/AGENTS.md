# Agent map — how-to/

You are in task recipes.

- Folder: `how-to/<slug>/` with `id: <slug>` and `kind: how-to`.
- One task, one outcome. If it needs a taught sequence, it belongs in `tutorials/`.
- Title the README as an imperative action.
- Link related tutorials by catalog `id`.
- Follow skill `add-catalog-entry` with `kind: how-to`.
