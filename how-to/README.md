# How-tos

Task-oriented recipes, not a sequence. Folder names are kebab-case and equal the catalog `id` (`how-to/time-a-kernel/`).

Title the README as an action (“Time a kernel”, “Read a CSV”). Link back to any tutorial on [the learning path](../docs/learning-path.md) that the reader should already know.
