---
name: add-exercise
description: Add a complete exercise (prompt, compiling starter, separate solution)
---

Add an **exercise** to cpp-toolbox.

1. Read and follow skill `add-catalog-entry` with `kind: exercise`.
2. Folder is `exercises/<topic>/<slug>/` with `prompt.md`, compiling `starter/`, and `solution/`.
3. No spoilers in `prompt.md`. Docs: dropdown on HTML, Solutions appendix for PDF.
4. Use any extra arguments the user typed after this command as the topic/`id`/title.
5. Finish with skill `review-catalog-entry`.
