---
name: add-project
description: Add a complete mini-project catalog entry (small end-to-end program)
---

Add a **mini-project** to cpp-toolbox.

1. Read and follow skill `add-catalog-entry` with `kind: project`.
2. Folder is `projects/<slug>/`. Several translation units are allowed; keep the first hour of reading small.
3. Link learning-path tutorials; do not duplicate them.
4. Use any extra arguments the user typed after this command as the `id`/title.
5. Finish with skill `review-catalog-entry`.
