# Agent map — docs/

You are in the Sphinx book source.

- `index.md` is the future homepage. Do not paste programs here; `literalinclude` marked regions from the matching leaf sources.
- Follow skill `write-scientific-docs` and [style.md](style.md).
- A new catalog entry needs a page in the same change. Until Sphinx is wired, still add the Markdown file.
- Generated output belongs in `_build/` (gitignored), never committed.
- Math and citations are welcome; CS jargon without an engineering consequence is not.
