# Agent map — examples/

You are in complete programs.

- Path: `examples/<topic>/<slug>/` with `topic` from `catalog.yaml`.
- Self-contained unless using a *documented* `toolbox/` header.
- `kind: example`. Do not use this tree for prompts (exercises) or copy-out starters (templates).
- Follow skill `add-catalog-entry` with `kind: example`.
