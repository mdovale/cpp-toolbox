# Examples

Complete, commented programs grouped by **topic** (`examples/numerics/slug/`). Topics are the controlled vocabulary in [`catalog.yaml`](../catalog.yaml).

Examples are copyable and should run as a single target. They are not the taught sequence (that is `tutorials/`) and not a prompt/solution pair (that is `exercises/`).
