# Placeholder. The root helper `add_toolbox_example()` will register this
# entry from catalog.yaml. Add a real target here only if the entry needs
# extra sources or dependencies the helper cannot express.
#
# See docs/build.md and BLUEPRINT.md.
