# Tutorials

Taught sequence. Read in order along [docs/learning-path.md](../docs/learning-path.md). Folder names are `NN-slug/` where `slug` equals the catalog `id`.

Each tutorial is a vertical slice: `main.cpp` (and any extra sources), leaf `README.md`, `catalog.yaml` row, docs page with `literalinclude`. See [CONTRIBUTING.md](../CONTRIBUTING.md).
