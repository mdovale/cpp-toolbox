# Agent map — tutorials/

You are in the taught sequence.

- Folder: `tutorials/NN-slug/` with `NN` matching [docs/learning-path.md](../docs/learning-path.md) when the tutorial is on the spine.
- `id` is `slug` without the numeric prefix.
- Fill the spine before one-off tutorials that are not on that list.
- One executable (or a tiny set of files). This is a lesson, not a project.
- Follow skill `add-catalog-entry` with `kind: tutorial`.
